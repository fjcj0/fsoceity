import type { NextConfig } from "next";
const nextConfig: NextConfig = {
  images: {
    domains: [
      'images.pexels.com',
      'images.unsplash.com',
      'randomuser.me',
      'picsum.photos',
      'res.cloudinary.com'
    ],
  },
};
export default nextConfig;